<div class="card">
    <div class="card-header bg-dark text-light">Level 1</div>
    <div class="card-body">
        <p>Using the SportsORM, complete all the following queries and show their results on Level1.cshtml.</p>
        <ol>
            <li>...all womens' leagues</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>League Name</th>
                    <th>League Sport</th>
                </tr>
                @foreach (var league in ViewBag.WomensLeagues)
                {
                    <tr>
                        <td>@league.Name</td>
                        <td>@league.Sport</td>
                    </tr>
                }
            </table>
            <li>...all leagues where sport is any type of hockey</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>League Name</th>
                    <th>League Sport</th>
                </tr>
                @foreach (var league in ViewBag.HockeyLeagues)
                {
                    <tr>
                        <td>@league.Name</td>
                        <td>@league.Sport</td>
                    </tr>
                }
            </table>
            <li>...all leagues where sport is something OTHER THAN football</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>League Name</th>
                    <th>League Sport</th>
                </tr>
                @foreach (var league in ViewBag.NoFootballLeagues)
                {
                    <tr>
                        <td>@league.Name</td>
                        <td>@league.Sport</td>
                    </tr>
                }
            </table>
            <li>...all leagues that call themselves "conferences"</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>League Name</th>
                    <th>League Sport</th>
                </tr>
                @foreach (var league in ViewBag.Conferences)
                {
                    <tr>
                        <td>@league.Name</td>
                        <td>@league.Sport</td>
                    </tr>
                }
            </table>
            <li>...all leagues in the Atlantic region</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>League Name</th>
                    <th>League Sport</th>
                </tr>
                @foreach (var league in ViewBag.AtlanticLeagues)
                {
                    <tr>
                        <td>@league.Name</td>
                        <td>@league.Sport</td>
                    </tr>
                }
            </table>
            <li>...all teams based in Dallas</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>Team Location</th>
                    <th>Team Name</th>
                </tr>
                @foreach (var team in ViewBag.DallasTeams)
                {
                    <tr>
                        <td>@team.Location</td>
                        <td>@team.TeamName</td>
                    </tr>
                }
            </table>
            <li>...all teams named the Raptors</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>Team Location</th>
                    <th>Team Name</th>
                </tr>
                @foreach (var team in ViewBag.RaptorTeams)
                {
                    <tr>
                        <td>@team.Location</td>
                        <td>@team.TeamName</td>
                    </tr>
                }
            </table>
            <li>...all teams whose location includes "City"</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>Team Location</th>
                    <th>Team Name</th>
                </tr>
                @foreach (var team in ViewBag.CityTeams)
                {
                    <tr>
                        <td>@team.Location</td>
                        <td>@team.TeamName</td>
                    </tr>
                }
            </table>
            <li>...all teams whose names begin with "T"</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>Team Location</th>
                    <th>Team Name</th>
                </tr>
                @foreach (var team in ViewBag.TNames)
                {
                    <tr>
                        <td>@team.Location</td>
                        <td>@team.TeamName</td>
                    </tr>
                }
            </table>
            <li>...all teams, ordered alphabetically by location</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>Team Location</th>
                    <th>Team Name</th>
                </tr>
                @foreach (var team in ViewBag.OrderedLocationTeams)
                {
                    <tr>
                        <td>@team.Location</td>
                        <td>@team.TeamName</td>
                    </tr>
                }
            </table>
            <li>...all teams, ordered by team name in reverse alphabetical order</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>Team Location</th>
                    <th>Team Name</th>
                </tr>
                @foreach (var team in ViewBag.ReverseOrderNameTeams)
                {
                    <tr>
                        <td>@team.Location</td>
                        <td>@team.TeamName</td>
                    </tr>
                }
            </table>
            <li>...every player with last name "Cooper"</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                </tr>
                @foreach (var player in ViewBag.CooperPlayers)
                {
                    <tr>
                        <td>@player.FirstName</td>
                        <td>@player.LastName</td>
                    </tr>
                }
            </table>
            <li>...every player with first name "Joshua"</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                </tr>
                @foreach (var player in ViewBag.JoshuaPlayers)
                {
                    <tr>
                        <td>@player.FirstName</td>
                        <td>@player.LastName</td>
                    </tr>
                }
            </table>
            <li>...every player with last name "Cooper" EXCEPT those with "Joshua" as the first name</li>
            <table class="table table-bordered table-striped">
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                </tr>
                @foreach (var player in ViewBag.CooperNotJoshPlayers)
                {
                    <tr>
                        <td>@player.FirstName</td>
                        <td>@player.LastName</td>
                    </tr>
                }
            </table>
            <li>...all players with first name "Alexander" OR first name "Wyatt"</li>
           <table class="table table-bordered table-striped">
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                </tr>
                @foreach (var player in ViewBag.AlexOrWyattPlayers)
                {
                    <tr>
                        <td>@player.FirstName</td>
                        <td>@player.LastName</td>
                    </tr>
                }
            </table>
        </ol>
    </div>
</div>